// constructor for todo item object
// properties: title, description, dueDate, priority, completed
function item(title,description,dueDate,priority,completed) {
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.priority = priority;
    this.completed = completed;
}

// constructor for projects


// dynamically insert new items into the taskList div, keyed by project


// move completed items into the completedList per project


// switch project displayed by dynamically changing what is displayed in taskList


// delete todo


// change priority of todo (options: normal or high(checked))

